{% if element is null %}{% else %}
{% set displayValue = '' ~ element %}
{% set metricName = element.getMetricType().getName() %}
**{{ metricName }}:** {% if ': ' in displayValue %}{% set parts = displayValue|split(': ') %}{{ parts[1] }} *({{ parts[0] }})*{% else %}{{ displayValue }}{% endif %}
{% if element.getMaxProblemLevel() > 0 %} — {{ element.getProblemMessages()|join('; ') }}{% endif %}

{% endif %}
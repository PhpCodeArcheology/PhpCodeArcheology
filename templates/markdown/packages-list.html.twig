{% include('parts/header.md.twig') %}

[Home](index.html) / Packages

# Packages

| Package |{% for header in tableHeaders %} {{ header.shortName }} |{% endfor %}

|---|{% for header in tableHeaders %}---|{% endfor %}

{% for package in packages %}
{% set metricCollectionId = package.getIdentifier().__toString() %}
| {{ package.getName() }} |{% for metricKey in listMetricKeys %}{% set data = package.get(metricKey) %} {{ data }} |{% endfor %}

{% endfor %}

{% include('parts/footer.md.twig') %}

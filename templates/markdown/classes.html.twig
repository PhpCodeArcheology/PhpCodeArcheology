{% include('parts/header.md.twig') %}

[Home](index.md) / Classes

# Classes

| Name |{% for header in tableHeaders %} {{ header.shortName }} |{% endfor %}

|---|{% for header in tableHeaders %}---|{% endfor %}

{% for class in classes %}
{% set classKey = class.getIdentifier().__toString() %}
{% set classLabel = class.getName() %}
| [{{ classLabel|length > 50 ? '...' ~ classLabel[-47:] : classLabel }}](classes/{{ classKey }}.md) |{% for metricKey in listMetricKeys %}{% set data = class.get(metricKey) %} {{ data }} |{% endfor %}

{% endfor %}

{% include('parts/footer.md.twig') %}

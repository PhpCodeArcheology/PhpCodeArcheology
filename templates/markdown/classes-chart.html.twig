{% include('parts/header.md.twig') %}

# Classes Overview

## Complexity vs Coupling

The interactive bubble chart (X = Efferent Coupling, Y = Cyclomatic Complexity, Size = Lines of Code) is available in the HTML report.

## Class Summary

| Class | Namespace | CC | Uses | LOC |
|---|---|---|---|---|
{% for class in classes %}
| [{{ class.get('singleName') }}](classes/{{ class.getIdentifier().__toString() }}.md) | {{ class.get('namespace').getValue()|default('global') }} | {{ class.get('cc').getValue()|default(0) }} | {{ class.get('uses').getValue()|length }} | {{ class.get('loc').getValue()|default(0) }} |
{% endfor %}

## Dependency Matrix

The interactive dependency matrix is available in the HTML report.

{% include('parts/footer.md.twig') %}

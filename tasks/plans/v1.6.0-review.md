# Code-Review v1.6.0 — Report-Verzeichnis-Isolation

**Datum:** 2026-03-24
**Reviewer:** Claude (Task 7)
**Gesamtbewertung:** 🟡 GELB — bereit zum Merge, zwei kleinere Befunde

---

## Testergebnis

```
Tests: 162 passed (480 assertions) — alle grün ✅
```

Beide neuen Testdateien werden vollständig ausgeführt:
- `ReportSubDirTest`: 5/5 ✅
- `MultiReportTypeTest`: 7/7 ✅

---

## Befunde pro Datei

### `src/Report/ReportTrait.php` 🟡
- `$reportSubDirName` und `getReportSubDir()` sind deklariert, aber **nirgends aufgerufen** (weder in eigenen Klassen noch in Tests). Totes Code.
- Nicht kritisch, aber es sollte entweder genutzt oder entfernt werden.

### `src/Report/HtmlReport.php` ✅
- `outputDir` korrekt auf `reportDir/html/` gesetzt.
- `mkdir` korrekt aus dem Konstruktor in `generate()` verschoben.
- `generateIndexPage()` liest `history.jsonl` aus `reportDir` (Root) — korrekt.

### `src/Report/MarkdownReport.php` ✅
- `outputDir` korrekt auf `reportDir/markdown/` gesetzt.
- `mkdir` vor `clearReportDir()` — korrekt.

### `src/Report/JsonReport.php` 🟡
- `mkdir` noch im Konstruktor statt in `generate()` — inkonsistent mit Html/Markdown.
- Folge: Unterverzeichnis wird angelegt, auch wenn `generate()` nie aufgerufen wird.
- Nicht kritisch für Korrektheit.

### `src/Report/SarifReport.php` 🟡
- Gleiche Inkonsistenz: `mkdir` im Konstruktor.

### `src/Report/AiSummaryReport.php` 🟡
- Gleiche Inkonsistenz: `mkdir` im Konstruktor.

### `src/Application/ArgumentParser.php` ✅
- Komma-Split korrekt implementiert.
- Einzelner Typ → String, mehrere → Array. Passt zur Abwärtskompatibilität.

### `src/Application/Config.php` ✅
- Validierung für String und Array korrekt.
- `VALID_REPORT_TYPES` enthält alle 5 Typen.

### `src/Report/ReportFactory.php` ✅
- `createMultiple()` korrekt: normalisiert auf Array, erzeugt separate Config-Clone pro Typ.
- Alle 5 Typen im match-Ausdruck.
- **Randnotiz (pre-existing):** `create()` hat Default `'markdown'` (Zeile 55), Application.php hat Default `'html'`. Kein Regressionsrisiko, da `createMultiple()` den Default aus Application erhält.

### `src/Application/Application.php` ✅
- Nutzt `ReportFactory::createMultiple()` ✅
- Iteration über Reports korrekt.
- Migrations-Hinweis: korrekt platziert (nach `generate()`, vor `writeHistory()`), prüft auf `index.html` im Root.
- Text des Hinweises ist klar und sinnvoll.

### `src/Application/Service/HistoryService.php` ✅ (unverändert)
- Pfad zu `history.jsonl` bleibt in `reportDir` Root — nicht in Unterverzeichnissen.
- Unverändert, keine Regressionen.

### `src/Report/ReportInterface.php` ✅ (unverändert)
- Signatur unverändert.

---

## Edge Cases

| Szenario | Verhalten | Bewertung |
|---|---|---|
| `reportDir` nicht existent | Application.php Zeile 171–175 erstellt es | ✅ |
| `--report-type` nicht gesetzt | Default 'html' in Application.php Zeile 160–162 | ✅ |
| Ungültiger Typ (`--report-type=pdf`) | Config::validate() wirft ConfigException | ✅ |
| `--report-type=html,html` (Duplikate) | Zwei HtmlReport-Instanzen, Report wird zweimal generiert | ⚠️ kein Fehler, aber unnötig |
| Einzelwert `--report-type=html` | Funktioniert (String → Config → createMultiple normalisiert) | ✅ |
| `history.jsonl` Pfad | Bleibt in reportDir Root (HistoryService + HtmlReport::generateIndexPage) | ✅ |

---

## Nachbesserungen

### Pflicht (Blocker): keine

### Optional (empfohlen):

**1. `getReportSubDir()` entfernen oder nutzen** (ReportTrait.php)
Die Methode ist deklariert aber nie aufgerufen. Entweder entfernen oder in `ReportFactory`/Tests verwenden.

**2. Konsistenz bei `mkdir`** (JsonReport, SarifReport, AiSummaryReport)
Entweder alle in den Konstruktor (aktuelle Praxis für diese 3) oder alle in `generate()` (HtmlReport/MarkdownReport). Aktuell gemischt.

**3. Deduplication in `createMultiple()`**
`array_unique($types)` vor der foreach-Schleife würde doppelte Typen ignorieren.

---

## Abschlussbewertung

Der Feature-Branch ist **technisch korrekt** und **bereit zum Merge**. Alle Tests sind grün, die Kernfunktionalität (Subdirectory-Isolation, Multi-Report, Abwärtskompatibilität, history.jsonl-Pfade) ist sauber implementiert. Die gefundenen Punkte sind kosmetischer/konsistenz-bezogener Natur ohne Auswirkung auf das Laufzeitverhalten.
